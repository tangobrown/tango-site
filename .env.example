# Contact form delivery is currently stubbed (see app/actions/contact.ts).
# When the destination is decided (open question #5: inbox vs CRM vs a
# scheduler like Cal.com), add its config here. For example, if you wire
# email via Resend:
#
# RESEND_API_KEY=
# CONTACT_TO_EMAIL=
